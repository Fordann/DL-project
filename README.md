# DL-project

update requirements.txt after adding a new package: pip freeze > requirements.txt 

If a model have already been saved: change its name and start training again

To start training: python3 main.py --training
